#include <iostream>
using namespace std;

void printUp(int n) {
    if (n == 0)
        return;

    printUp(n - 1);
    cout << n << " ";
}

int main() {
    int n;

    cout << "Enter n: ";
    cin >> n;

    printUp(n);

    return 0;
}
